# Security Policy

## 受信源码模型

ChengyuLang 会将 `.cy` 翻译为 Python，并使用 `compile()` / `exec()` 执行。
这意味着 `.cy` 拥有与当前 Python 进程相同的权限。项目不提供沙箱，也不应被用来
运行陌生人提交、网络下载或未经审查的源码。

## 内置运行时

内置实现遵循“危险动作默认不发生”的原则。涉及文件系统根目录、进程信号、部署、
无限等待或无限循环的原始脑洞，都被替换为 dry-run、白名单或有界操作。

## 报告问题

请通过仓库的私密安全报告功能提交可能导致非预期文件写入、进程控制、任意模块加载
或翻译器绕过的问题。在问题修复前，请勿公开可直接利用的演示代码。

